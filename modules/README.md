# Shared Module Example Sources

This directory contains shared module example sources used by the kernel examples profile setup.

Source tree:

- `modules/src/main/java/io/govaryn/kernel/examples/modules`

The kernel build includes this source directory via `build-helper-maven-plugin`.
